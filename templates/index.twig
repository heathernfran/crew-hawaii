{% extends "base.twig" %}

{% block content %}
	<!-- <h2>{{ title }}</h2> -->
	<p>{{ content }}</p>
	{% for post in posts %}
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
	{% endfor %}
{% endblock %}
